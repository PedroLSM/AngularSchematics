{
    "$schema": "http://json-schema.org/schema",
    "id": "NavigationSchematics",
    "title": "Navigation Options Schema",
    "type": "object",
    "description": "Configuração de componentes de navegação padrão do Angular Material",
    "properties": {
        "auth": {
            "type": "boolean",
            "default": "n",
            "x-prompt": "Configurar autenticação?"
        },
        "project": {
            "type": "string",
            "description": "Generate in specific Angular CLI workspace project"
        }
    },
    "required": [
        "auth"
    ]
}